# Database Configuration
# For Mac with Homebrew PostgreSQL (no password, use your Mac username):
DATABASE_URL=postgresql://YOUR_MAC_USERNAME@localhost:5432/pulsemap
# Example: DATABASE_URL=postgresql://jensholm@localhost:5432/pulsemap
#
# For PostgreSQL with password:
# DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/pulsemap
#
# For Docker:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pulsemap

# Politiloggen API (public, no authentication required)
POLITILOGGEN_API_URL=https://api.politiet.no/politiloggen/v1

# Mock Data Mode (for testing without API)
# Set to "true" to use mock data instead of real API
# Set to "false" to use real Politiloggen data (default)
USE_MOCK_DATA=false

# Geocoding Service (Nominatim)
NOMINATIM_API_URL=https://nominatim.openstreetmap.org
NOMINATIM_USER_AGENT=PulseMap/1.0 your-email@example.com

# Optional: If using Google Geocoding API instead
# GOOGLE_GEOCODING_API_KEY=your_api_key_here

# Map Settings
NEXT_PUBLIC_MAP_CENTER_LAT=59.9139
NEXT_PUBLIC_MAP_CENTER_LNG=10.7522
NEXT_PUBLIC_MAP_DEFAULT_ZOOM=12
NEXT_PUBLIC_REFRESH_INTERVAL_MS=60000
NEXT_PUBLIC_OSLO_DISTRICT=Oslo

# API Rate Limiting
API_RATE_LIMIT_PER_MINUTE=60
GEOCODING_RATE_LIMIT_PER_SECOND=1
